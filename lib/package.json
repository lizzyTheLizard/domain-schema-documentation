{
  "name": "lib",
  "scripts": {
    "build": "tsc && cpy 'src/**/*.hbs' ../dist && cpy 'src/**/*.yaml' ../dist",
    "check": "yarn run check:types && yarn run check:test",
    "check:test": "jest",
    "check:types": "tsc --noEmit --skipLibCheck",
    "tests": "jest --watch"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "handlebars": "^4.7.8",
    "minimist": "^1.2.8",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/minimist": "^1.2.5",
    "@types/node": "^20.11.30",
    "@types/tmp": "^0.2.6",
    "cpy-cli": "^5.0.0",
    "jest": "^29.7.0",
    "tmp": "^0.2.1",
    "ts-jest": "^29.1.2",
    "tsx": "^4.7.1",
    "typescript": "^5.3.0"
  }
}
